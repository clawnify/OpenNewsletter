<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <rect width="64" height="64" rx="14" fill="#DD5164"/>
  <!-- The app's glyph, the same one it publishes to the Clawnify sidebar
       (`mail`). Stroked, so the sidebar's outline pass keeps it as drawn. -->
  <g fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" transform="translate(14 14) scale(1.5)">
    <path d="m22 7-8.991 5.727a2 2 0 0 1-2.009 0L2 7"/>
    <rect x="2" y="4" width="20" height="16" rx="2"/>
  </g>
</svg>
